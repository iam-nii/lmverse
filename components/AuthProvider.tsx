// "use client";

// import React, { useEffect } from "react";
// // import { setupAuthListener } from "@/store/api/authApi";

// export const AuthProvider = ({ children }: { children: React.ReactNode }) => {
//     useEffect(() => {
//         const subscription = setupAuthListener();

//         return () => {
//             subscription.unsubscribe();
//         };
//     }, []);

//     return <>{children}</>;
// };
