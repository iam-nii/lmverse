import { useAuthStore } from "@/store/AuthStore";
import type { PropsWithChildren } from "react";

export default function StudentsLayout({ children }: PropsWithChildren) {
 
  return <>{children}</>;
}
